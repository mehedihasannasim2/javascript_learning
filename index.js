// value will change if it call the variable again

let interestRate = 0.3; 
interestRate = 1;
console.log(interestRate)


// if we dont need to reassign than we should use CONST

const interestRate2 = 0.3; 
console.log(interestRate)